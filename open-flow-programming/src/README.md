The file topology.py contains the two topologies for the exercises.

The first topology can be started with:
sudo mn --custom topology.py --topo topo2 --mac

The second one can be started with:
sudo mn --custom topology.py --topo topo3 --mac
