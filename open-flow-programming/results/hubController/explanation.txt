a. results can be seen in hub_ping1.txt - average time is 32ms; Because the controller floods the packets to every port except the incoming
one, every switch and every host observe traffic generated by the ping.

b. results can be seen in hub_ping2.txt - average time is 34ms; effects are similar with the ones from a.

c.

*** Ping: testing ping reachability
h1 -> h2 h3 h4 h5 
h2 -> h1 h3 h4 h5 
h3 -> h1 h2 h4 h5 
h4 -> h1 h2 h3 h5 
h5 -> h1 h2 h3 h4 
*** Results: 0% dropped (0/20 lost)


